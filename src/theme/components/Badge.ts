import { defineStyleConfig } from '@chakra-ui/react';

const Badge = defineStyleConfig({
  baseStyle: {
    width: 'full',
    paddingY: '3px',
    fontSize: '11px',
    borderRadius: '10px',
  },
});

export default Badge;
